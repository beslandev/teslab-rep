import { BottomTabRoutes } from '../navigation/BottomTab/types/routes';

import { TEXT } from './text';

export const LABELS = {
  [BottomTabRoutes.MAIN]: TEXT.main.label,
  [BottomTabRoutes.AUTO]: TEXT.auto.label,
  [BottomTabRoutes.SERVICE]: TEXT.service.label,
  [BottomTabRoutes.RIDES]: TEXT.rides.label,
  [BottomTabRoutes.MARKET]: TEXT.market.label,
};
